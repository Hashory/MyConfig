# =====================================================================
# Dotter Local Configuration File (Example)
# =====================================================================
# This file is a template for `local.toml`, which handles machine-specific (OS, etc.) settings.
# To use it, copy this file and rename it to `local.toml`.
# (* `local.toml` itself is added to .gitignore so it is not tracked by Git)

# ---------------------------------------------------------------------
# Loading other configuration files (OS-specific paths, etc.)
# ---------------------------------------------------------------------
# Uncomment the following if you want to use Windows-specific deployment paths.
# includes = [".dotter/windows.toml"]
includes = []

# ---------------------------------------------------------------------
# Specifying packages to install/deploy
# ---------------------------------------------------------------------
# Specify the package names defined in `global.toml` as an array.
# List only the packages you want to enable on this machine.
packages = ["git", "jj", "micro", "cargo", "vscode", "antigravity", "cursor"]
